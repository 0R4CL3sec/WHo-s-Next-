// Supabase connection, shared by index.html (the wheel) and admin.html (the dashboard).
// URL and anon/publishable key: Supabase → Project Settings → API. Never put the
// service_role / secret key here — this file is readable by anyone who has the page.
// Leave both empty to keep everything in the browser only (the dashboard needs them).
const SUPABASE_URL = "https://qhjrdvyqjsovshxjdzic.supabase.co";
const SUPABASE_KEY = "sb_publishable_frsBYeju6duR2o23GWcM9Q_qc5efoaB";
